# 2026-09-07 動作確認

## 2026-09-08 GUI拡張

- GUI・設定ファイル・アプリ生成の関連テストは **26 passed** (12.95秒)。
  GUIテストは共通のTclインタープリター上でウィンドウを分離して実行。
- 実ウィンドウでジャイロイド生成と断面表示を確認。
- 最小サイズ1020×700で生成・保存・中止・結果欄が表示されることを確認。
- 設定JSONの検証、姿勢の復元、Z=0からの断面、キャンセル後の再実行、
  保存完了を待つ終了動作をテスト。
- キャンセルは工程間で確認する。実行中のBoolean処理を強制終了するものではない。

## 3DBenchyの読み込み（2026-09-08）

- ユーザー提供の11,285,384バイトのSTLを同梱。コピー前後のSHA-256が一致。
- 頂点溶接後に頂点番号が重複する552面だけを除外し、225,154面の閉じた
  ソリッドとして読み込み。頂点座標・外形寸法・元ファイルのバイト列を保持。
- 読み込み・同梱モデル・GUI・設定・アプリの関連テスト **57件が成功**。
  細い有効面の保持、入力メッシュの非破壊性、穴あきモデルの拒否も検証。
- wheelをビルドし、STLと出典情報の収録、ソースツリーを使わない
  wheel内リソースからの3DBenchy読み込みを確認。
- 実画面のボタンから読み込み、船体とZ断面を確認。プレビューの面の間引きを
  廃止し、表示上の穴を解消。変更後のGUIテスト6件も成功（8.84秒）。
- この確認はインポートが対象。3DBenchyでのサポート・インフィル生成や
  Simplify3Dでのスライス・実機印刷は未検証。

## 初期版の確認

Windows / Python 3.12、trimesh 4.6.8、Manifold 3.5.2、scikit-image 0.26.0、
Matplotlib 3.11.1で確認。作業用 `.venv` は既存のインストール済みライブラリを参照する
`--system-site-packages` で作成し、プロジェクトをeditable installした。
クリーン環境用のセットアップ手順はREADMEに記載。

- 全体テスト: **254 passed** (374.13秒)。この実行開始時はGUIテスト追加前。
- 最終のアプリ・GUI・入力読込テスト: **43 passed** (10.18秒)。
  全体テストとの重複を除くと255件を検証。
- Tkのイベントループで別スレッド生成、選択モードの維持、書き出し、
  設定変更時の古い生成結果の無効化を確認。
- 画面経由で片持ち板と16 mmキューブを生成し、STL一式を保存。
- インストール済みCLIからダイヤモンドを生成・保存。
- インフィルでは3パターンの閉じた体積、内部空洞、元モデルの貫通穴、外形保持、
  設定の不正値と格子数上限を検証。
- サポートではBoolean結合、ベッド下へのはみ出し防止、先端上端、モデルとの非交差、
  STL再読み込み、サポート不要モデルを検証。

## 確認した例

- 片持ち板＋ツリーサポート
- 16 mmキューブ＋ジャイロイド
- 16 mmキューブ＋ダイヤモンド
- [サポートのプレビュー](images/support-preview.png)
- [ジャイロイドの断面プレビュー](images/infill-preview.png)

生成したSTLはローカルの `out/examples/` に保存し、Gitには含めない。
アプリのデモモデルから同様の形状を生成できる。

## 未検証

Simplify3D v5の実アプリでのスライス、G-code、実機印刷、クリーンPCでのセットアップ、
大型・任意形状の全般的な品質は未検証。STLは試験用の開発版出力。
GUIの3D描画はMatplotlibの面ソートによる表示上の限界があるため、内部構造は
Z断面でも確認する。サポートの全三角形同士の衝突や押出経路を網羅的に保証しない。
